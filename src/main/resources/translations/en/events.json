{
  "events": {
    "service_operation": {
      "todolist": {
        "tasks": {
          "create": {
            "name": "Create task",
            "pending": "Creating task {request.name}",
            "success": "Task {request.name} was created",
            "failure": "Unable to create task {request.name}"
          },
          "delete": {
            "name": "Delete task",
            "pending": "Deleting task {request.name}",
            "success": "Task {request.name} was deleted",
            "failure": "Unable to delete task {request.name}"

          }
        }
      }
    }
  }
}